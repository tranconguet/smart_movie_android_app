<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layoutMovie"
    android:layout_width="@dimen/cast_item_width"
    android:layout_height="@dimen/cast_item_height"
    android:layout_marginStart="@dimen/cast_item_margin_horizontal"
    android:layout_marginEnd="@dimen/cast_item_margin_horizontal"
    android:layout_marginTop="@dimen/cast_item_margin_vertical"
    android:background="@color/white">

    <ImageView
        android:id="@+id/ivCastImage"
        android:layout_width="match_parent"
        android:layout_height="@dimen/cast_image_height"
        android:scaleType="centerCrop"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/tvCastName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textColor="@color/black"
        android:textSize="@dimen/cast_item_name_text_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/ivCastImage" />

</androidx.constraintlayout.widget.ConstraintLayout>